// jest-dom añade matchers de Jest personalizados para hacer aserciones sobre nodos del DOM.
// Esto te permite hacer cosas como:
// expect(element).toBeInTheDocument();
// Es una buena práctica importarlo aquí para que esté disponible en todos los tests.
import '@testing-library/jest-dom';